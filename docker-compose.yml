version: '3.8'

services:
  api:
    build: .
    container_name: log-triager-api
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=development
      # Placeholder for Phase 2/3 ENV vars
      # - VECTOR_DB_URL=http://chromadb:8000
      # - LLM_API_KEY=${LLM_API_KEY}
    volumes:
      - .:/app
    restart: unless-stopped
    # depends_on:
    #   - chromadb # To be added in Phase 2
